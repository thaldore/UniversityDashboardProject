namespace UniversityDashBoardProject.Domain.Enums
{
    public enum TargetDirection
    {
        Positive = 1,    // Pozitif hedef (artırım istenen)
        Negative = 2     // Negatif hedef (azaltım istenen)
    }
}
